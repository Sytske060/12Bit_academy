let hoofdstadNL;
let hoofdstadDE;
let hoofdstadFR;

hoofdstadNL = " Amsterdam";
hoofdstadDE = " Berlijn";
hoofdstadFR = " Parijs";

const message = `De hoofdsteden van NL, DE en FR zijn:${hoofdstadNL},${hoofdstadDE},${hoofdstadFR}`;
console.log(message);
